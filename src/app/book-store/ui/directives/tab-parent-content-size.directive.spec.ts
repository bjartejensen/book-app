import { TabParentContentSizeDirective } from './tab-parent-content-size.directive';

describe('TabParentContentSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TabParentContentSizeDirective();
    expect(directive).toBeTruthy();
  });
});
